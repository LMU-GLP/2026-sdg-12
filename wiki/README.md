# wiki/

This folder is empty on purpose. Pages here get written *from* files in
`sources/` — see `../CLAUDE.md` for the rules.

Nothing goes in `wiki/` until there's a source behind it. Next steps for this
team, in order:

1. Split up and claim a source each (news story, dataset, UN targets, an
   existing product/program) on the fast fashion angle described in
   `../CLAUDE.md`, one file per source in `../sources/`.
2. Run "Ingest everything in sources/ and update the wiki" to build these
   pages for real, once sources exist.
3. Expect at least: an entity page per candidate country (Indonesia,
   Cambodia, Vietnam), a page on the fast fashion supply chain connecting
   consumption elsewhere to pollution/waste in Southeast Asia, and a
   comparison page across the three candidates to support picking one.
